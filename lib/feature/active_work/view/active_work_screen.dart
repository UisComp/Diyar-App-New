import 'package:flutter/material.dart';

class ActiveWorkScreen extends StatelessWidget {
  const ActiveWorkScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}