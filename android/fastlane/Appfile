# Path to the Google Play service-account JSON. In CI this is written from a
# secret to fastlane/play-service-account.json (see ci/CI_SETUP.md).
json_key_file(ENV["PLAY_JSON_KEY_FILE"] || "fastlane/play-service-account.json")
package_name("com.uis.diyar")
